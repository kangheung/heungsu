<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
		"-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="com.hotel.member.MemberDAO">
	
	<!-- 아이디 중복 검사 -->
	<select id="selectOne" parameterType="String" resultType="int">
		 select count(*) from member WHERE userid = #{userid}
	</select>
	
	<insert id="insertMember" parameterType="member">
	insert into member
		(userid, userpw, name, email, gender,pnum, birthYear, birthMonth, birthDay)
	values
		(
			#{userid}, #{userpw}, #{name}, #{email},#{gender}, #{pnum},
			#{birthYear}, #{birthMonth}, #{birthDay}
		)
	</insert>

</mapper>





